DATABASE_URL="postgresql://applyflow:applyflow@localhost:5432/applyflow?schema=public"
# Use Neon's pooled URL for DATABASE_URL and its direct URL for migrations.
DIRECT_URL="postgresql://applyflow:applyflow@localhost:5432/applyflow?schema=public"
AUTH_SECRET="replace-with-openssl-rand-base64-32"
AUTH_GOOGLE_ID="replace-with-google-client-id"
AUTH_GOOGLE_SECRET="replace-with-google-client-secret"
AUTH_URL="http://localhost:3000"
NEXTAUTH_URL="http://localhost:3000"

# Enable Google Calendar API for the OAuth client.
# The app requests https://www.googleapis.com/auth/calendar.readonly at login.
# Enable Gmail API for the OAuth client.
# The app requests https://www.googleapis.com/auth/gmail.readonly at login.

# Zero-additional-cost AI extraction. ApplyFlow never falls back to paid OpenAI APIs.
AI_PROVIDER="groq"
AI_MODEL="openai/gpt-oss-120b"
AI_REASONING_EFFORT="high"
AI_DAILY_TOKEN_BUDGET="180000"
GROQ_API_KEY=""

# Internal signature shared by the Netlify scheduler and background worker.
# Generate at least 32 random bytes and keep this server-side.
EMAIL_MONITOR_WORKER_SECRET=""

# Optional future integrations
SENTRY_DSN=""
POSTHOG_KEY=""
